// backend/src/index.ts

/*
 * This is the main entry point for our backend application.
 * For now, its only job is to print a message to the console
 * to confirm that our TypeScript setup is working correctly.
 */

const main = () => {
  console.log("Hello, Financial Tracker Backend!");
  console.log("TypeScript setup is running successfully! (Etapa 1 - 8%)");
};

// Execute the main function
main();
